import { LdapDirective } from './ldap.directive';

describe('LdapDirective', () => {
  it('should create an instance', () => {
    const directive = new LdapDirective();
    expect(directive).toBeTruthy();
  });
});
